<?php# jSale v1.431# http://jsale.biz# Подключение настроекinclude_once dirname(__FILE__) . '/../config.inc.php';# Вывод ошибокif ($config['errors'] === true){	error_reporting(E_ALL); # Уровень вывода ошибок	ini_set('display_errors', 'on'); # Вывод ошибок включён}# Логирование ошибокif ($config['error_logging'] === true){	ini_set("log_errors", 'on'); # Логирование включено	ini_set("error_log", dirname(__FILE__) . '/error_log.txt'); # Путь файла с логами}if ($config['partner']['enabled'] !== true){	echo 'Партнёрская программа отключена администратором.';	die;}# Подключение модулейinclude_once dirname(__FILE__) . '/../modules/M_DB.inc.php';$mDB = M_DB::Instance();# Кодировкаheader('Content-type: text/html; charset=' . $config['encoding']);# Формирование GET запроса (на случай PHP как CGI)parse_str(parse_url($_SERVER['REQUEST_URI'], PHP_URL_QUERY), $_GET);# Обработка имени файла$file = str_replace(dirname(__FILE__) . '/', '', (__FILE__));$file = str_replace('.php', '', $file);$no_header = true;if (isset($_POST['restore']) && isset($_POST['email'])){	if (!$partner = $mDB->GetItemsByParam('partner', 'email', $_POST['email']))	{		$message = 'В базе нет партнёра с таким email. Возможно вы ошиблись.';				$content = '		<form method="post" class="form-inline">			<p>Введите ваш email здесь:				<input type="text" name="email" value="" class="input-medium" placeholder="Ваш email">				<button class="btn" name="restore">Ввести</button>			</p>		</form>';	}	else	{		$partner = $partner[0];		include_once dirname(__FILE__) . '/../modules/M_Email.inc.php';		$mEmail = M_Email::Instance();				$сontent = $mEmail->PreparePartnerRestore($partner['email'], $partner['password'], $partner['code'], $config);				$mEmail->SendEmail($partner['email'], $config['email']['answer'], $config['email']['subjectPartnerRegister'], $сontent, $config['email']['answerName'], $config['encoding']);				$content = '		<p>Данные доступа отправлены на ваш email.</p>		<p>Можете войти в <a href="index.php">свой кабинет</a>, используя свой email и пароль.</p>		';	}}else{	$content = '	<h1>Введите ваш email</h1><br/>	<form method="post" class="form-inline">		<p>Введите ваш email здесь:			<input type="text" name="email" value="" class="input-medium" placeholder="Ваш email">			<button class="btn" name="restore">Ввести</button>		</p>	</form>';}# Вывод дизайнаinclude_once dirname(__FILE__) . '/_menu.php';include_once dirname(__FILE__) . '/../design/partnerCreate.tpl.php';